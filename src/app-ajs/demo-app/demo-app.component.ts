'use strict';

// Register `demoApp` component, along with its associated controller and template
angular.
  module('demoApp').
  component('demoApp', {
    templateUrl: './demo-app.template.html'
  });
